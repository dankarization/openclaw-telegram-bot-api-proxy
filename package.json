{
  "name": "openclaw-telegram-bot-api-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Telegram Bot API proxy для OpenClaw с приоритетом локального API и безопасным cloud fallback.",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "for file in src/*.mjs test/*.test.mjs test/support/*.mjs spikes/001-durable-state-storage/*.mjs; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22.16.0 <23"
  }
}
