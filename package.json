{
  "name": "openclaw-telegram-bot-api-proxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Telegram Bot API proxy для OpenClaw с приоритетом локального API и безопасным cloud fallback.",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "node --check src/telegram-bot-api-proxy.mjs && node --check test/telegram-bot-api-proxy.test.mjs",
    "test": "node --test test/telegram-bot-api-proxy.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
