{
  "name": "openclaw-telegram-bot-api-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production local-first Telegram Bot API proxy for OpenClaw with guarded cloud fallback.",
  "scripts": {
    "check": "npm run lint && npm test",
    "lint": "for file in src/*.mjs test/*.test.mjs; do node --check \"$file\" || exit 1; done",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
